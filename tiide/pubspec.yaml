name: tiide
description: "Tiide — distress/craving session timer."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2

  drift: ^2.21.0
  drift_flutter: ^0.2.4
  sqlite3_flutter_libs: ^0.5.26
  path_provider: ^2.1.5
  path: ^1.9.0

  flutter_local_notifications: ^18.0.1
  flutter_background_service: ^5.1.0
  flutter_background_service_android: ^6.3.0
  flutter_background_service_ios: ^5.0.3
  home_widget: ^0.7.0
  app_links: ^6.3.2
  workmanager: ^0.5.2
  timezone: ^0.9.4
  shared_preferences: ^2.3.2

  # S4: Biometric + Geo
  health: ^11.1.0
  geolocator: ^13.0.2
  permission_handler: ^11.3.1
  fl_chart: ^0.70.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  drift_dev: ^2.21.0
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
